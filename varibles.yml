- name: nginx service needs to be installed and run
  hosts: Nodes
  become: yes
  tasks:
    - name: nginx service needs to be installed and run
      ansible.builtin.dnf:
        name: "{{ item }}"
        state: latest
      loop:
        - mysql
        - nginx
        - zip
